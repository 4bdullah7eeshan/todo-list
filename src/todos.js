// Todo object creator

const createTodo = () => {
    // Factory code
    const title = '';
    const description = '';
    const dueDate = '';
    const priority = '';
    const notes = '';
    const checklist = '';
    return { title, description, dueDate, priority, notes, checklist }
}

